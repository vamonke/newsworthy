// Leaderboard popup: top 10, all time, each player's best round (GET /api/leaderboard, worker/src/leaderboard.js).
// Every round with a score is posted by the Worker, so there is nothing to submit here.
// Approved in news-leaderboard-design.html.
const $=s=>document.querySelector(s),dollars=n=>'$'+Number(n||0).toLocaleString();
// Solid 24×24 trophy in currentColor, gold, silver and bronze for the top 3.
const TROPHY='<svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M6 2h12v2h3.5v2.5A5 5 0 0 1 17.2 11.4 6 6 0 0 1 13 14.8V17h3.5v2h-9v-2H11v-2.2a6 6 0 0 1-4.2-3.4A5 5 0 0 1 2.5 6.5V4H6zm0 4H4.5v.5a3 3 0 0 0 1.8 2.8A6 6 0 0 1 6 8zm12 0v2a6 6 0 0 1-.3 1.3 3 3 0 0 0 1.8-2.8V6z"/><path d="M6 20h12v2.5H6z"/></svg>';
const PLACES=['gold','silver','bronze'];
function place(rank){const n=document.createElement('span');n.className='lb-n';if(rank<=3){n.classList.add(PLACES[rank-1]);n.innerHTML=TROPHY;n.setAttribute('aria-label','#'+rank);}else n.textContent=rank;return n;}
function photoCard(p){const card=document.createElement('div');card.className='recap-card';const frame=document.createElement('span');frame.className='np-print';const img=new Image();img.src=p.src;img.alt='';img.loading='lazy';const tag=document.createElement('strong');tag.className='recap-tag';tag.textContent=dollars(p.earned);frame.append(img,tag);const title=document.createElement('b');title.textContent=p.headline;card.append(frame,title);return card;}
function openPlayer(row,rank){$('#lb-player-rank').textContent=`#${rank} all time`;const name=$('#lb-player-name');name.replaceChildren();if(rank<=3)name.append(place(rank));name.append(row.name+(row.you?' (you)':''));$('#lb-player-total').textContent=dollars(row.total);$('#lb-player-photos').replaceChildren(...row.photos.map(photoCard));$('#lb-player').showModal();}
function render(board){const list=$('#lb-rows');list.replaceChildren();for(const [i,row] of board.top.entries()){const li=document.createElement('li'),button=document.createElement('button');button.className='lb-row'+(row.you?' you':'');const who=document.createElement('b');who.textContent=row.name+(row.you?' (you)':'');const thumbs=document.createElement('span');thumbs.className='lb-thumbs';for(const p of row.photos.slice(0,3)){const img=new Image();img.src=p.src;img.alt='';img.loading='lazy';thumbs.append(img);}const money=document.createElement('strong');money.className='np-money';money.textContent=dollars(row.total);button.append(place(i+1),who,thumbs,money);button.onclick=()=>openPlayer(row,i+1);li.append(button);list.append(li);}$('#lb-status').textContent=board.top.length?'':'No scores yet. Play a round to be the first.';}
// From the results popup, the board opens on top of it: Back returns there and Play again starts a round.
// From the header it only has Close. `round` marks the player's row.
export async function openLeaderboard({round,onPlay}={}){const dialog=$('#scores-dialog');$('#lb-back').hidden=$('#lb-again').hidden=!onPlay;$('#lb-close').hidden=Boolean(onPlay);$('#lb-again').onclick=()=>{dialog.close();onPlay?.();};$('#lb-rows').replaceChildren();$('#lb-status').textContent='Loading…';if(!dialog.open)dialog.showModal();try{const r=await fetch('/api/leaderboard'+(round?'?round='+encodeURIComponent(round):''),{signal:AbortSignal.timeout(15000)});const board=await r.json();if(!r.ok)throw new Error(board.error||'Couldn’t load the leaderboard.');render(board);}catch(e){$('#lb-status').textContent=e.name==='TimeoutError'?'Couldn’t load the leaderboard. Try again.':e.message;}}
$('#lb-back').onclick=$('#lb-close').onclick=()=>$('#scores-dialog').close();
